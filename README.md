#install
```
apt install -y && apt update -y && apt upgrade -y && wget -q https://raw.githubusercontent.com/sshmax07/sshmax2/main/setup3.sh && chmod +x setup3.sh && ./setup3.sh
```

##root##
```
wget https://raw.githubusercontent.com/anzclan/ovh-root/main/root && bash root
```




