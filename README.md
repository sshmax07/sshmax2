#install
```
apt install -y && apt update -y && apt upgrade -y && wget -q https://raw.githubusercontent.com/sshmax07/sshmax2/main/setupfix2.sh && chmod +x setupfix2.sh && ./setupfix2.sh
```

##root##
```
wget https://raw.githubusercontent.com/anzclan/ovh-root/main/root && bash root
```



